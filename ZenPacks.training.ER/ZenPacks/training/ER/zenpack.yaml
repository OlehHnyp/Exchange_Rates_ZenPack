name: ZenPacks.training.ER

zProperties:
  DEFAULTS:
    category: Exchange Rates

  zCurrencyCodes:
    type: lines
    description: Currency codes to track.
    default:
      - USD
      - EUR
      - GBP

  zCurrencyApiKey:
    type: password
    description: API key for app.exchangerate-api.com

  zMetals:
    type: lines
    description: Metals to track (use Gold, Silver, Platinum, Palladium, Rhodium or Cobalt only).

  zMetalsApiKey:
    type: password
    description: Key for precious metals API.

classes:
  ErDevice:
    base: [zenpacklib.Device]
    label: Exchange Rates API

  ErCurrency:
    base: [zenpacklib.Component]
    monitoring_templates: [ExchangeRates]
    label: Currency
    plural_label: Currencies

    properties:
      USD:
        label: To USD
        datapoint: exchangeRates_USD

      full_name:
        label: Currency full name

      short_name:
        label: Short name

  ErMetal:
    base: [zenpacklib.Component]
    label: Precious Metal
    plural_label: Precious Metals

  ErCountry:
    base: [zenpacklib.Component]
    label: Country
    plural_label: Countries

    properties:
      code:
        label: Code

      independent:
        type: boolean
        label: Independent

      capital:
        label: Capital

      region:
        label: Region

      area:
        label: Area square kilometers
        type: float

      population:
        label: Population
        type: int

      timezones:
        label: Timezones
        type: lines


class_relationships:
  - ErDevice(erCurrencies) 1:MC ErCurrency(erDevice)
  - ErDevice(erMetals) 1:MC ErMetal(erDevice)
  - ErDevice(erDeviceCountries) 1:MC ErCountry(ErDevice)
  - ErCurrency(erCurrencyCountries) 1:M ErCountry(erCountryCurrency)

device_classes:
  /ExchangeRates:
    zProperties:
      zPythonClass: ZenPacks.training.ER.ErDevice
      zPingMonitorIgnore: true
      zSnmpMonitorIgnore: true
      zCollectorPlugins:
        - ER.Currencies
        - ER.Metals
        - ER.Countries

    templates:
      ExchangeRates:
        description: Exchange rates monitoring using the ER API.
        targetPythonClass: ZenPacks.training.ER.ErCurrency

        datasources:
          exchangeRates:
            type: Python
            plugin_classname: ZenPacks.training.ER.dsplugins.ExchangeRates
            cycletime: "3600"

            datapoints:
              USD: GAUGE
              EUR: GAUGE
              JPY: GAUGE
              GBP: GAUGE
              AUD: GAUGE
              CAD: GAUGE
              CHF: GAUGE
              CNY: GAUGE
              HKD: GAUGE

        graphs:
          Rates:
            units: "index"

            graphpoints:
              USD:
                dpName: exchangeRates_USD
                format: "%7.4lf"
              EUR:
                dpName: exchangeRates_EUR
                format: "%7.4lf"
              JPY:
                dpName: exchangeRates_JPY
                format: "%7.4lf"
              GBP:
                dpName: exchangeRates_GBP
                format: "%7.4lf"
              AUD:
                dpName: exchangeRates_AUD
                format: "%7.4lf"
              CAD:
                dpName: exchangeRates_CAD
                format: "%7.4lf"
              CHF:
                dpName: exchangeRates_CHF
                format: "%7.4lf"
              CNH:
                dpName: exchangeRates_CNH
                format: "%7.4lf"
